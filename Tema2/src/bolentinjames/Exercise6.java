package bolentinjames;

//import java.util.Scanner;

public class Exercise6 {

	public static void main(String[] args) {
/*
		Scanner sc = new Scanner(System.in);

		int moneyToPay;

		int moneyGiven;

		int finalMoney;

		int euro;

		int twoEuro;

		int fiftyCents;

		int twentyCents;

		int tenCents;

		int fiveCents;

		int twoCents;

		int oneCent;

		System.out.println("How much does the client have to pay?");

		moneyToPay = sc.nextInt();

		System.out.println("How much the client pay?");

		moneyGiven = sc.nextInt();

		finalMoney = moneyGiven - moneyToPay;

		if (finalMoney == 0) {
			System.out.println("The exchamge is 0");

		} else {

			for (int i = finalMoney; i > 0; i--) {

				if (i >= 200) {

					twoEuro = i / 200;
					i -= 200;

				} else if (i >= 100) {
					euro = i / 100;
					i -= 100;
				} else if (i >= 50) {
					fiftyCents = i / 50;
					i -= 50;
				} else if (i >= 20) {
					twentyCents = i / 20;
					i -= 20;
				} else if (i >= 10) {
					tenCents = i / 10;
					i -= 10;
				} else if (i >= 5) {
					fiveCents = i / 5;
					i -= 5;
				} else if (i >= 2) {
					twoCents = i / 2;
					i -= 2;
				} else {
					oneCent = i;
					i -= 1;
				}
			}
		}
		// System.out.println(twoEuro + "monedas de dos euros" + euro + "monedas de un
		// euro" + fiftyCents + "monedas de 50 céntimos" + twentyCents + "monedas de 20
		// céntimos" + tenCents + "monedas de 10 céntimos" + fiveCents + "monedas");

		sc.close();
*/
	}
}
